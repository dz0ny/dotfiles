# Nix build outputs
result
result-*

# direnv
.direnv/

# devenv (if used)
.devenv/

# macOS Finder metadata
.DS_Store

# Local overrides/secrets
.env
.envrc.local

# Generated report from scripts/capture-state.sh (the script is tracked, its
# large output is not).
scripts/capture-state.out